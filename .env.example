# Copy to .env and fill in your values
# The frontend reads VITE_WORKER_URL at build time (optional - user can set in Settings)
VITE_WORKER_URL=https://prospectai-worker.YOUR_SUBDOMAIN.workers.dev

# Worker secret - set via: wrangler secret put ANTHROPIC_API_KEY
# ANTHROPIC_API_KEY=sk-ant-...
